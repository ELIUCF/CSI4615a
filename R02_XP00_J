// R02_XP00_J.java (non-compliant)
// Expressions: Do not ignore values returned by methods
import java.io.File;

public class R02_XP00_J {
    public void deleteFile() {
        File someFile = new File("someFileName.txt");
        // do something...
        someFile.delete(); // return value ignored — may fail silently
    }
}

// This noncompliant code example attempts to delete a file but fails to check whether the operation has succeeded
